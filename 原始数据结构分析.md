# 原始数据结构分析（jdhydevicedb）

> 目的：梳理客户初始化数据库 `jdhydevicedb` 的关键表结构与样本数据特征，为后续稳定映射与迭代提供依据。  
> 范围：`device_list`、`sensors_list`、`03001idb_icedriftbuoy`、`03004imb_icemassbalancebuoy`。

## 1. 数据库概况

已连接库：`jdhydevicedb`  
识别到的主要表（节选）：
- `03001idb_icedriftbuoy`
- `03004imb_icemassbalancebuoy`
- `device_list`
- `sensors_list`
- 以及 `03002~03008` 等其他设备类型业务表

说明：当前 POC 重点表与现有系统映射逻辑一致，核心来源为 `03001` 与 `03004`。

---

## 2. 关键表规模与时间分布

### 2.1 行数统计
- `device_list`：107 行
- `sensors_list`：12 行
- `03001idb_icedriftbuoy`：306,611 行
- `03004imb_icemassbalancebuoy`：212,657 行

### 2.2 设备维度（distinct devid）
- `03001`：56 个设备
- `03004`：14 个设备

### 2.3 时间范围
- `03001.time`：最小 `0200-01-01 00:54:17`，最大 `2026-02-07 08:20:00`
- `03004.time`：最小 `0199-12-31 23:54:44`，最大 `2026-02-07 08:02:48`

结论：存在明显异常早期时间（年份 199/200），需在导入与统计阶段做脏数据识别/过滤策略。

---

## 3. 各表结构与业务意义

## 3.1 `device_list`（设备主数据/清单）
主要字段：
- `name`：设备名称
- `devid`：设备编号（逻辑主键候选）
- `iridiumid`：铱星编号
- `sensorflag`：传感器组合标识
- `latflag/lat/lonflag/lon`：设备坐标（可能为空）
- `workstate`：工作状态（注释显示为状态字段）
- `display`：是否展示标志位
- `ownership`：归属信息

分析：
- 该表更像“设备台账/配置表”，适合映射到系统设备基础信息维度。
- `devid` 是后续关联业务表的关键连接点。
- 坐标字段在样本中有空值，不能作为唯一定位来源。

## 3.2 `sensors_list`（传感器字典/协议说明）
主要字段：
- `id`、`name`、`type`
- `nameofthevariable`：变量名
- `example`：示例报文
- `analysis`：解析说明
- `data_type`
- `备注`（当前库中列名存在中文编码显示异常）

分析：
- 该表属于“协议/字典”层，不是时序观测数据。
- 可作为后续前端字段解释、单位展示、报文解析文档来源。
- 当前字符集显示存在中文乱码，建议后续核对连接编码与库表编码策略。

## 3.3 `03001idb_icedriftbuoy`（冰漂浮标原始时序）
主要字段：
- 标识：`id`, `sn`, `devid`, `iridiumid`
- 时间：`db_time`（入库时间）, `time`（解析时间）
- 定位：`latflag/lat/lonflag/lon`
- 指标：`board_voltage`, `board_temp`, `atmosphere`
- 北斗扩展：`bd_time`, `bd_posflag`, `bd_latflag/bd_lat`, `bd_lonflag/bd_lon`

数据质量特征：
- `lat/lon` 空值数：0（完整性较好）
- 经度样本出现 `260.x`，说明存在 0~360 表达体系，地图展示前需统一标准（例如转为 -180~180）

## 3.4 `03004imb_icemassbalancebuoy`（冰质量平衡浮标原始时序）
在 `03001` 基础上增加了多传感器字段：
- 环境：`air_temp`, `air_humid`, `atmosphere`
- 风场：`wind_speed`, `wind_direct`
- 温度链：`tempC150`, `tempC200_1`, `tempC200_2`, `tempC_pt`
- 声呐：`sonar_on`, `sonar_under`

数据质量特征：
- `lat/lon` 空值数：45,672（占比约 21.5%）
- 部分记录为“分包上报”（如温度链字段与基础环境字段分批出现）

分析：
- 该表信息量更丰富，适合形成设备详情页“扩展指标”能力。
- 因分包与空值较多，导入时需允许部分字段为空，避免整包丢弃。

---

## 4. 现阶段可落地的映射结论（面向POC）

1. **设备主键**：统一使用 `devid` 关联设备维表。  
2. **观测时间**：优先使用 `time` 作为业务时间，`db_time` 作为审计时间。  
3. **坐标处理**：
   - 按 `latflag/lonflag` 处理南北/东西符号；
   - 增加经度规范化（0~360 -> -180~180）策略。  
4. **幂等键建议**：`devid + sn + time`。  
5. **空值策略**：`03004` 中坐标或某些传感器缺失不应阻断整条导入。  
6. **异常时间策略**：对年份过小（如 < 2000）记录标记异常并按策略过滤或降权。  

---

## 5. 风险与后续建议（仅分析，不改代码）

1. **编码风险**：中文字段注释/内容存在乱码，后续需统一字符集链路（数据库、连接参数、客户端）。
2. **时间脏数据风险**：历史异常时间会影响在线率和轨迹时间轴。
3. **坐标体系风险**：经度存在大于 180 值，地图展示可能偏移或穿越。
4. **分包数据风险**：`03004` 数据包字段不完整是常态，不应按“全字段齐全”作为有效条件。
5. **多源扩展风险**：`03002~03008` 等其他表尚未纳入统一映射，后续扩展需按同样方法做字段梳理。

---

## 6. 设备详情展示字段梳理建议（按设备类型分层）

> 目标：在需求尚未完全明确时，依据原始字段名先建立“可解释、可扩展”的设备详情展示模型。

### 6.1 统一骨架（所有设备）
建议设备详情固定为 4 区块：
1. **设备基础信息**（`device_list`）
   - 设备编号 `devid`
   - 设备名称 `name`
   - 铱星编号 `iridiumid`
   - 归属区域 `ownership`
   - 工作状态 `workstate`
2. **定位与时效**（业务表最新记录）
   - 最新时间 `time`
   - 经纬度（经 `latflag/lonflag` 归一）
   - 在线状态（24小时规则）
3. **核心观测指标**（按类型模板化）
4. **轨迹与历史趋势**

### 6.2 类型A：IDB（`03001idb_icedriftbuoy`）
**定位**：基础漂移浮标

**核心字段**：
- `board_voltage`（板电压）
- `board_temp`（板温）
- `atmosphere`（大气压）
- `lat/lon`（定位）
- `bd_*`（北斗辅助定位）

**详情建议**：
- 默认显示：板电压、板温、大气压、最新定位
- 扩展显示：北斗定位状态（bd_time 是否有值）
- 趋势优先：板温、大气压

### 6.3 类型B：IMB（`03004imb_icemassbalancebuoy`）
**定位**：多传感器浮标（冰质量平衡）

**核心字段**：
- `air_temp`（空气温度）
- `air_humid`（空气湿度）
- `atmosphere`（大气压）
- `wind_speed/wind_direct`（风速风向）
- `sonar_on/sonar_under`（声呐）
- `tempC150/tempC200_1/tempC200_2/tempC_pt`（温度链）

**详情建议**：
- 默认显示：空气温度、湿度、压力、风速风向
- 扩展显示：声呐指标
- 高级显示：温度链（折叠或弹层）
- 趋势优先：空气温度、湿度、风速

### 6.4 `device_list` 多类型匹配规则（建议）
在需求未明确前，建议使用可回退规则：
1. 优先看 `sensorflag`
   - 包含 `idb` -> IDB 模板
   - 包含 `imb` -> IMB 模板
2. 若 `sensorflag` 不可靠，按是否在业务表有数据判断
   - `03004` 有数据 -> IMB
   - 否则 `03001` 有数据 -> IDB
3. 两表都无数据 -> 仅展示设备基础信息
4. `03002~03008` 后续逐表补模板

### 6.5 字段分级展示（避免页面冗余）
- **L1 默认可见**（6~8项）：时间、经纬度、在线状态、温度、压力、风速
- **L2 展开可见**：电压、湿度、风向、声呐
- **L3 高级字段**：温度链、北斗扩展、原始字段

### 6.6 中文命名建议（前端）
- `devid` -> 设备编号
- `name` -> 设备名称
- `ownership` -> 归属区域
- `iridiumid` -> 铱星编号
- `board_voltage` -> 板电压
- `board_temp` -> 板温
- `air_temp` -> 空气温度
- `air_humid` -> 空气湿度
- `atmosphere` -> 大气压
- `wind_speed` -> 风速
- `wind_direct` -> 风向
- `sonar_on` -> 雪深声呐
- `sonar_under` -> 冰底声呐

### 6.7 本轮分析可执行结论
1. 设备详情应按“类型模板”渲染，而不是所有字段平铺。
2. 先稳定 IDB / IMB 两套模板。
3. 预留 `03002~03008` 模板扩展位。
4. 后续实现建议“字段配置驱动”，降低硬编码和冗余。

---

## 7. 本文档用途

- 作为 POC 阶段“原始数据认知基线”
- 作为后续映射规则、导入质量校验、可视化解释的依据
- 作为你后续提需求时的结构化参考
- 作为设备详情分类型展示实现的前置分析依据
