# 邮箱模块迁移计划

## 目标
将现有基于 Flask 的 `email_pro` 邮箱处理模块迁移并整合到当前 Django + Vue 项目中，在“用户管理”下新增“邮箱管理”模块，保留原有核心功能与交互。

---

## 迁移计划（先确认，无代码改动）

### 1. 盘点现有功能与边界（已完成）
- 服务方式：Flask 单体（`email_pro/app.py`）+ 原生 HTML/JS（`templates/index.html`、`static/main.js`）
- 邮箱服务：163 IMAP（`imap.163.com:993`），授权码登录
- 核心能力：
  1) 登录（`/api/login`）
  2) 邮件查询（`/api/emails`，日期范围、分页，最大 730 天）
  3) 下载附件（`/api/download`，并发下载、按主题尾号建目录）
  4) 邮件正文（`/api/email_detail`）
  5) 删除已下载邮件（`/api/delete_downloaded`）
- 业务规则：
  - 邮件主题末尾需包含连续数字，否则下载/删除时跳过
  - 附件目录：`downloads/<主题末尾数字>/`
  - 并发数：16，下载/删除均为并发执行
- 前端交互：登录 → 日期筛选 → 查询列表 → 批量下载/删除 → 正文查看

### 2. 数据与接口映射设计（最小落库方案）
- 最小落库目标：保留账号配置、任务日志、下载/删除记录，不保存邮件正文与附件二进制。
- 建议数据表（Django 模型）：
  1) `email_account`：`id`, `user_id`, `email`, `imap_host`, `imap_port`, `auth_code(加密)`, `is_active`, `created_at`, `updated_at`
  2) `email_task_log`：`id`, `user_id`, `email`, `action`(login/query/download/delete/detail), `request_params`, `result`, `created_at`
  3) `email_attachment_log`：`id`, `user_id`, `email`, `email_id`, `subject`, `folder_name`, `file_name`, `status`(success/skip/fail), `created_at`
- 接口路由设计（Django）：
  - `POST /email/login/`
  - `POST /email/list/`（查询邮件）
  - `POST /email/detail/`（正文）
  - `POST /email/download/`（下载附件）
  - `POST /email/delete_downloaded/`（删除已下载邮件）
- 权限规则：与现有用户体系一致，管理员可查看所有账号日志，普通用户仅可访问自己的邮箱记录。

### 3. 后端迁移方案（执行中）
- 新增 Django 应用：`apps/email`，承接 IMAP 登录、查询、附件下载、正文、删除逻辑。
- 复用 `email_pro/imap_client.py` 的核心逻辑，改造为 Django 可复用服务类（移除 Flask 依赖，保留 163 IMAP 配置）。
- 鉴权：沿用现有 token/session 机制，接口需用户登录态。
- 账号处理：登录成功后写入 `email_account`（加密授权码），用于后续操作；不在内存持久化连接。
- 日志：
  - `email_task_log` 记录每次请求（action、参数、结果摘要）。
  - `email_attachment_log` 记录每个附件下载/跳过/失败。
- 限制规则：查询时间范围保持 730 天限制；附件目录规则保持 `downloads/<主题尾号>/`。
- 错误处理：保持原有错误提示风格，统一返回 `{code, msg, data}`。

### 4. 前端迁移方案（执行中）
- 菜单：在“用户管理”下新增“邮箱管理”入口（仅管理员可见，后续可按需求放开）。
- 路由：新增 `/email_manage`，页面 `EmailManage.vue`。
- 页面功能：
  1) 账号登录/保存（邮箱 + 授权码）
  2) 日期范围查询（默认近 1 个月，保留 730 天限制提示）
  3) 列表展示（日期/发件人/主题/正文展开）
  4) 批量选择、批量下载附件、删除已下载邮件
  5) 操作日志面板（显示任务结果与统计）
- UI：沿用 ElementUI 表格、分页、弹窗，整体风格对齐现有管理端。

### 5. 配置与部署（执行中）
- 配置项（建议放 `settings.py` / 环境变量）：
  - `EMAIL_IMAP_HOST=imap.163.com`
  - `EMAIL_IMAP_PORT=993`
  - `EMAIL_ATTACHMENT_DIR=/data/email_downloads`（替代当前相对路径 `downloads`）
  - `EMAIL_MAX_WORKERS=16`
  - `EMAIL_MAX_QUERY_DAYS=730`
- 服务器要求：放行 993 端口到 163 IMAP（出站），磁盘预留附件空间。
- 部署文档更新：新增“邮件模块配置”章节与磁盘空间评估说明。

### 6. 验证与回归（执行中）
- 核心验收：登录成功/失败、日期查询、分页、正文、下载、删除。
- 规则验收：730 天限制、主题尾号目录规则、重复附件跳过。
- 权限验收：管理员可查看所有日志，普通用户仅自身。
- 可靠性：并发下载/删除在 16 线程下稳定性与超时处理。

---

## 需要你确认的信息
- `email_pro` 的功能范围（例如：邮箱账号管理 / 邮件发送 / 模板 / 任务 / 日志等）。
- 使用的邮件服务方式（SMTP/第三方平台）。
- 是否需要保留原有数据库结构或可以迁移重建。
- 期望的页面入口与功能优先级。

确认计划后，我会开始按计划执行迁移。