# 数据库表结构（精简版）

## 1. test 库（平台控制）

### 表：`t_user`
- `id`
- `name`
- `password`
- `type`（1管理员/2客户）
- `device_list`（JSON，授权设备ID）
- `is_delete`
- `create_time`
- `update_time`

用途：登录、权限与设备授权控制。

---

## 2. jdhydevicedb（原始业务库）

### 表：`device_list`
主要字段：
- `devid`（设备ID）
- `name`（设备名称）
- `sensorflag`（设备类型标识）
- `iridiumid`
- `latflag/lat/lonflag/lon`
- `workstate`
- `ownership`

用途：设备主数据展示。

### 表：`03001idb_icedriftbuoy`
主要字段：
- `sn`
- `devid`
- `time`
- `iridiumid`
- `latflag/lat/lonflag/lon`
- `board_voltage`
- `board_temp`
- `atmosphere`

用途：基础浮标业务数据。

### 表：`03004imb_icemassbalancebuoy`
主要字段：
- `sn`
- `devid`
- `time`
- `iridiumid`
- `latflag/lat/lonflag/lon`
- `board_voltage`
- `board_temp`
- `air_temp`
- `air_humid`
- `atmosphere`
- `wind_speed`
- `wind_direct`

用途：扩展浮标/站点业务数据。

---

## 3. 当前约束
- 平台不写入上述原始业务表
- 平台只读取并做展示聚合
- 权限来自 test 库的 `t_user.device_list`

---

## 4. 性能索引（首页设备上图/最新位置查询）

> 目的：优化 `/device/list/?use_latest_position=1` 场景，避免大表上 `devid + time` 查询超时。

### 4.1 建索引 SQL（MySQL）

```sql
-- 设备主表：用于设备清单查询与排序
ALTER TABLE `device_list`
ADD INDEX `idx_device_list_devid` (`devid`);

-- 原始时序表 03001：用于按设备取最新时间/最新坐标
ALTER TABLE `03001idb_icedriftbuoy`
ADD INDEX `idx_03001_devid_time` (`devid`, `time`);

-- 原始时序表 03004：用于按设备取最新时间/最新坐标
ALTER TABLE `03004imb_icemassbalancebuoy`
ADD INDEX `idx_03004_devid_time` (`devid`, `time`);
```

### 4.2 执行前检查（避免重复创建）

```sql
SHOW INDEX FROM `device_list`;
SHOW INDEX FROM `03001idb_icedriftbuoy`;
SHOW INDEX FROM `03004imb_icemassbalancebuoy`;
```

### 4.3 回滚 SQL（如需）

```sql
ALTER TABLE `device_list` DROP INDEX `idx_device_list_devid`;
ALTER TABLE `03001idb_icedriftbuoy` DROP INDEX `idx_03001_devid_time`;
ALTER TABLE `03004imb_icemassbalancebuoy` DROP INDEX `idx_03004_devid_time`;
```