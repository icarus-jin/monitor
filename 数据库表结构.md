# 数据库表结构说明（当前实现版）

## 1. 核心表

## 1.1 `t_user`（用户权限表）

字段：
- `id` 主键
- `name` 用户名
- `password` 密码（哈希）
- `type` 用户类型（1 管理员，2 客户）
- `device_list` JSON，授权设备ID列表
- `is_delete` 软删除标记
- `create_time` 创建时间
- `update_time` 更新时间

用途：登录鉴权、设备权限控制。

---

## 1.2 `device_list`（设备主表）

关键字段：
- `id` 序号
- `name` 设备名称
- `devid` 设备ID
- `iridiumid` 铱星号
- `sensorflag` 传感器标志位
- `latflag`/`lat` 纬度标记与值
- `lonflag`/`lon` 经度标记与值
- `workstate` 工作状态
- `display` 同步标志位
- `ownership` 归属单位

用途：设备列表、设备权限过滤、设备基础信息。

---

## 2. 业务时序表映射

后端按 `devid` 前5位映射业务表（用于最新数据/趋势/轨迹查询）。

当前映射（代码内）：

- `01001 -> 01001gnssstation`
- `02001 -> 02001ugms_unattendedglaciermonitoringsystem`
- `02002 -> 02002ms_meteorologicalstation`
- `02003 -> 02003spom_spacephysicsobservationmodule`
- `02004 -> 02004sib_seaicebuoy`
- `02005 -> 02005pmc_penguinmonitoringcamera`
- `02006 -> 02006cr_cornerreflector`
- `02007 -> 02007stb_southtempchainbuoy`
- `03001 -> 03001idb_icedriftbuoy`
- `03002 -> 03002tcb_tempchainbuoy`
- `03003 -> 03003uis_unmannedicestation`
- `03004 -> 03004imb_icemassbalancebuoy`
- `03005 -> 03005mp_meltpond`
- `03006 -> 03006ib_imagebuoy`
- `03007 -> 03007ab_adcpbuoy`
- `03008 -> 03008isb_icestressbuoy`

---

## 3. 查询依赖的公共字段约定

业务表查询能力依赖以下字段：

1. 最新时间计算：`devid`, `time`
2. 轨迹计算：`devid`, `time`, `lat`, `lon`（可选 `latflag`, `lonflag`）
3. 趋势明细：按表全字段读取（排除 `id`, `devid`）

若某业务表缺少上述字段，相关能力会降级为空结果。

---

## 4. 索引建议

为保障性能，建议每个业务时序表建立：

- `(devid, time)` 复合索引

`device_list` 建议索引：

- `devid`
- （可选）`name`、`ownership`（若搜索频繁）


USE jdhydevicedb;

ALTER TABLE `01001gnssstation` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `02001ugms_unattendedglaciermonitoringsystem` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `02002ms_meteorologicalstation` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `02003spom_spacephysicsobservationmodule` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `02004sib_seaicebuoy` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `02005pmc_penguinmonitoringcamera` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `02006cr_cornerreflector` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `02007stb_southtempchainbuoy` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `03001idb_icedriftbuoy` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `03002tcb_tempchainbuoy` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `03003uis_unmannedicestation` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `03004imb_icemassbalancebuoy` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `03005mp_meltpond` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `03006ib_imagebuoy` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `03007ab_adcpbuoy` ADD INDEX `idx_devid_time` (`devid`, `time`);
ALTER TABLE `03008isb_icestressbuoy` ADD INDEX `idx_devid_time` (`devid`, `time`);

-- 设备主表建议索引（若不存在）
ALTER TABLE `device_list` ADD INDEX `idx_devid` (`devid`);
ALTER TABLE `device_list` ADD INDEX `idx_name` (`name`);
ALTER TABLE `device_list` ADD INDEX `idx_ownership` (`ownership`);