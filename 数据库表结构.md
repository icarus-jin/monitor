# 数据库表结构（精简版）

## 1. test 库（平台控制）

### 表：`t_user`
- `id`
- `name`
- `password`
- `type`（1管理员/2客户）
- `device_list`（JSON，授权设备ID）
- `is_delete`
- `create_time`
- `update_time`

用途：登录、权限与设备授权控制。

---

## 2. jdhydevicedb（原始业务库）

### 表：`device_list`
主要字段：
- `devid`（设备ID）
- `name`（设备名称）
- `sensorflag`（设备类型标识）
- `iridiumid`
- `latflag/lat/lonflag/lon`
- `workstate`
- `ownership`

用途：设备主数据展示。

### 表：`03001idb_icedriftbuoy`
主要字段：
- `sn`
- `devid`
- `time`
- `iridiumid`
- `latflag/lat/lonflag/lon`
- `board_voltage`
- `board_temp`
- `atmosphere`

用途：基础浮标业务数据。

### 表：`03004imb_icemassbalancebuoy`
主要字段：
- `sn`
- `devid`
- `time`
- `iridiumid`
- `latflag/lat/lonflag/lon`
- `board_voltage`
- `board_temp`
- `air_temp`
- `air_humid`
- `atmosphere`
- `wind_speed`
- `wind_direct`

用途：扩展浮标/站点业务数据。

---

## 3. 当前约束
- 平台不写入上述原始业务表
- 平台只读取并做展示聚合
- 权限来自 test 库的 `t_user.device_list`
