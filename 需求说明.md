# 项目需求说明（当前实现版）

## 1. 项目目标

建设“气象监测设备管理与数据分析平台”，满足以下核心目标：

1. 多设备统一管理（设备清单、状态、归属）
2. 多角色权限控制（管理员/客户）
3. 设备数据可视化分析（最新值、轨迹、趋势、明细）
4. 首页地图快速定位与查看设备详情

---

## 2. 角色与权限

### 2.1 超级管理员（type=1）
- 可查看全部用户与设备
- 可管理用户（新增、编辑、删除、重置密码）
- 可访问所有设备数据

### 2.2 客户用户（type=2）
- 仅可查看被授权设备（`t_user.device_list`）
- 仅可访问授权设备的最新/趋势/轨迹数据

---

## 3. 功能需求清单

## 3.1 登录与鉴权
- 用户名密码登录
- 登录成功返回 token
- 前端路由拦截，未登录禁止访问业务页

## 3.2 首页地图（Welcome）
- 2D/3D 地图切换
- 设备点位展示
- 显示设备统计（总数、在线、离线）
- 点击设备点打开全屏设备详情层（无需离开首页）

## 3.3 用户管理
- 用户列表分页与按名称搜索
- 新增用户
- 编辑用户类型/关联设备
- 删除与批量删除（软删除）
- 重置密码

## 3.4 设备管理
- 设备列表分页、关键词、归属、在线状态筛选
- 查看设备详情（最新数据）
- 时间范围选择（1天/7天/30天/1年/自定义）
- 时间联动分析：
  - 轨迹图
  - 折线图
  - 明细表
- 轨迹播放（播放/暂停、上一点、下一点）

## 3.5 地图内嵌详情模式
- `device_list?device_id=xxx&embed=1`
- 隐藏设备列表，仅展示设备详情分析面板

---

## 4. 非功能需求

1. 数据权限：后端必须按用户授权过滤设备
2. 响应效率：热点接口支持缓存与索引优化
3. 可维护性：接口统一返回结构（code/msg/data）
4. 可扩展性：按设备前缀映射业务表，支持新增设备类型

---

## 5. 当前范围边界

1. 设备新增/编辑/删除接口保留但当前禁用（原始库只读策略）
2. 数据写入接口保留但当前禁用（业务数据只读）
3. 核心工作集中在“读、查、分析、可视化”
