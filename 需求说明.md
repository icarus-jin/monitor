# 气象监测系统需求说明（精简基线）

## 1. 当前目标
构建一个**设备数据展示平台**：
- 用户与权限来自 `test` 库
- 设备与业务数据全部来自原始库 `jdhydevicedb`
- 平台以展示为主，不承担业务数据写入

## 2. 角色与权限

### 2.1 超级管理员（type=1）
- 可查看全部设备与全部业务数据

### 2.2 客户用户（type=2）
- 仅可查看 `t_user.device_list` 授权设备
- 可查看授权设备的列表、详情、趋势、轨迹

## 3. 功能范围（保留）
1. 登录与会话控制
2. 设备列表展示（筛选：名称/ID、区域、类型、在线状态）
3. 设备详情展示：
   - 最新数据
   - 趋势数据（24h/7d）
   - 轨迹地图（含时间轴）
4. 首页概览展示

## 4. 功能范围（去除）
1. 设备新增/编辑/删除
2. 业务数据写入/导入入口
3. 非必要演示脚本（导入链路）

## 5. 核心规则
1. 在线规则：最后上报时间 24 小时内为在线
2. 坐标规则：按 `latflag/lonflag` 修正南北/东西，且经度归一化到 [-180,180]
3. 权限规则：客户仅能访问授权 `devid`

## 6. 后续扩展（暂不在本轮）
- 告警中心
- 高级3D地图
- 分享外链
