# 接口说明

统一返回：

```json
{
  "code": 200,
  "msg": "success",
  "data": {}
}
```

---

## 用户模块（/user）

- `POST /user/login/`
- `GET /user/user_list/`
- `POST /user/register/`
- `PUT /user/register/`
- `DELETE /user/register/`
- `POST /user/batch_delete/`
- `PUT /user/reset_password/`
- `GET /user/reset_password/`

---

## 设备模块（/device）

- `GET /device/list/`
- `GET /device/simple_list/`
- `GET /device/map/points/`
- `GET /device/data/latest/`
- `GET /device/data/trend/`
- `GET /device/track/`
- `GET /device/overview/`
- `GET /device/map/tile/`

只读策略下禁用：
- `POST /device/detail/`
- `PUT /device/detail/`
- `DELETE /device/detail/`
- `POST /device/batch_delete/`
- `POST /device/data/ingest/`

---

## 数据接收模块（/receive_log）

- `GET /receive_log/list/`
- `POST /receive_log/send_tcp/`
