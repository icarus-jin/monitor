# 接口说明

> 本文档描述当前项目实际在用接口，按模块划分。

## 返回结构

所有接口统一返回：

```json
{
  "code": 200,
  "msg": "success",
  "data": {}
}
```

---

## 用户模块（/user）

- `POST /user/login/` 登录
- `GET /user/user_list/` 用户列表（分页/搜索）
- `POST /user/register/` 新增用户
- `PUT /user/register/` 编辑用户
- `DELETE /user/register/` 删除用户（软删）
- `POST /user/batch_delete/` 批量删除用户（软删）
- `PUT /user/reset_password/` 重置密码
- `GET /user/reset_password/` 重置密码（兼容）

---

## 设备模块（/device）

- `GET /device/list/` 设备列表（分页/筛选/状态）
- `GET /device/simple_list/` 设备简表（关联设备选择）
- `GET /device/map/points/` 首页地图点位
- `GET /device/data/latest/` 设备最新数据
- `GET /device/data/trend/` 设备趋势数据（明细/图表）
- `GET /device/track/` 设备轨迹
- `GET /device/overview/` 首页概览统计
- `GET /device/map/tile/` 地图瓦片代理

### 只读策略下禁用接口

- `POST /device/detail/`
- `PUT /device/detail/`
- `DELETE /device/detail/`
- `POST /device/batch_delete/`
- `POST /device/data/ingest/`
