# 架构说明（精简版）


管理员账号：admin
管理员密码：Admin@123456
测试用户：demo_user
测试密码：Demo@123456

## 1. 总体架构
- 后端：Django
- 前端：Vue2 + Element UI
- 地图：Leaflet + OpenStreetMap
- 数据源：
  - `test`：用户与权限（`t_user`）
  - `jdhydevicedb`：设备与业务数据（原始表）

## 2. 数据职责划分

### 2.1 test 库（业务控制）
- `t_user`：登录、角色、设备授权列表（`device_list`）

### 2.2 jdhydevicedb（业务展示）
- `device_list`：设备主数据
- `03001idb_icedriftbuoy`：原始浮标数据
- `03004imb_icemassbalancebuoy`：原始浮标/站点扩展数据

## 3. 关键设计原则
1. 展示平台只读：设备和业务数据不在平台侧改写
2. 权限前置：所有设备相关接口按 `t_user.device_list` 过滤
3. 原始直读：设备详情、趋势、轨迹、概览均实时读取原始库

## 4. 主要接口
- `/device/list/`：设备列表（原始库）
- `/device/simple_list/`：设备简表
- `/device/data/latest/`：最新数据
- `/device/data/trend/`：趋势数据（24h/7d）
- `/device/track/`：轨迹点
- `/device/overview/`：概览数据

## 5. 已精简内容
- 删除平台内原始数据导入命令
- 关闭设备新增/编辑/删除与业务写入接口
- 设备页面收敛为纯展示流程
