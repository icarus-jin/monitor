# 项目架构说明

## 1. 总体架构

本项目为前后端分离架构：

- 前端：Vue2 + ElementUI + ECharts + Leaflet + Mars3D
- 后端：Django（View 风格接口）
- 数据库：MySQL（业务主库 + 原始设备数据表）

逻辑分层：

1. 展示层（Frontend）
2. 业务接口层（Backend API）
3. 数据访问层（Django ORM + 原始库 SQL）
4. 存储层（`t_user`、`device_list`、各前缀业务表）

---

## 2. 前端架构

### 2.1 路由

- `/login` 登录页
- `/home/welcome` 首页地图
- `/user_list` 用户管理
- `/device_list` 设备管理与设备详情分析

### 2.2 核心页面职责

1. `Welcome.vue`（首页）
   - 2D/3D 地图切换
   - 设备点位展示（接口：`/device/map/points/`）
   - 点击设备点弹出全屏详情层（内嵌 `#/device_list?device_id=xxx&embed=1`）

2. `User.vue`（用户管理）
   - 用户增删改查、重置密码
   - 关联设备选择（复用 `/device/simple_list/`）

3. `DeviceInfo.vue`（设备管理）
   - 设备列表查询
   - 设备详情弹窗：最新数据、轨迹图、趋势图、明细表
   - 时间联动：日期范围同时驱动轨迹、图表、明细
   - `embed=1` 模式下隐藏列表，仅保留详情展示

---

## 3. 后端架构

### 3.1 应用划分

- `apps/user`：认证与用户管理
- `apps/device`：设备列表、地图点位、概览、数据详情/趋势/轨迹

### 3.2 鉴权机制

- 登录后签发 token（`token_store`）
- 接口通过请求头 token + session 识别用户
- 用户类型：
  - `type=1` 超级管理员
  - `type=2` 客户（按 `device_list` 限制可见设备）

### 3.3 设备数据路由机制

- 根据设备 `devid` 前5位映射到具体业务表
- 最新数据、趋势数据、轨迹数据均按映射表读取

---

## 4. 数据流（关键链路）

### 4.1 首页设备点位

`Welcome.vue` -> `/device/map/points/` ->
后端按权限取 `device_list` -> 按前缀映射业务表取最新时间和最新有效经纬度 -> 返回点位列表。

### 4.2 设备详情分析

`DeviceInfo.vue` ->
- `/device/data/latest/`（最新字段）
- `/device/data/trend/`（明细+图表）
- `/device/track/`（轨迹）

前端统一时间条件并发请求，实现联动分析。

### 4.3 用户关联设备

`User.vue` -> `/device/simple_list/` 获取可选设备 -> 提交 `device_list` 到 `/user/register/`（新增/编辑）。

---

## 5. 部署与运行建议

1. 前后端分域部署时确保 API 代理与 token 透传一致。
2. 原始业务表建议建立 `(devid, time)` 组合索引，优化趋势/轨迹查询。
3. 首页地图瓦片代理接口建议结合磁盘缓存（项目已内置）。
