后端：django
前端：vue2
中间件：mysql
前端目录：frontend/web
后端目录：backend/api_server

## 后端 utils 目录说明

通用工具统一放在 `backend/utils/` 目录下，供各 app 复用。包括：

### token（utils/token.py）
- Token 的生成、存储、校验、删除
- 开发环境使用内存存储，生产环境建议使用 Redis

### log（utils/log.py）
- 统一日志格式
- 用于请求、异常、业务操作记录

### message（utils/message.py）
- 统一 API 响应格式（工具类，仅成功/失败）
- `success(data, msg)`：成功响应
- `error(msg, code, status)`：失败响应

### json_utils（utils/json_utils.py）
- 请求体解析：`parse_body(request)`，支持 JSON 和 form-urlencoded
- 参数获取：`get_param(body, key, default)`，兼容 POST 的 list 值取首项

### 使用示例
```python
from utils import token_store, success, error, parse_body, get_param, logger

# 响应（成功/失败）
return success(data={'list': []})
return error('参数错误', code=400)

# 请求解析
body = parse_body(request)
name = get_param(body, 'name')

# 日志
logger.info('用户登录: %s', username)
logger.exception('请求异常')
```