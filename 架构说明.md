# 项目架构说明

## 总体架构

- 前端：Vue2 + ElementUI + ECharts + Leaflet + Mars3D
- 后端：Django（API + TCP 接收）
- 数据库：MySQL（业务主库 + 原始设备数据表）

分层结构：
1. 展示层（前端页面）
2. 业务接口层（Django API）
3. 接收层（TCP 接收 + 解析 + 入库）
4. 数据访问层（ORM + 原始库 SQL）
5. 存储层（业务表/日志表）

---

## 前端模块

- `/home/welcome` 首页地图与设备详情弹层
- `/user_list` 用户管理
- `/device_list` 设备详情分析（最新/轨迹/趋势/明细）
- `/receive_log` 接收日志与 TCP 测试

---

## 后端模块

- `apps/user`：登录与用户管理
- `apps/device`：设备列表/地图点位/数据查询
- `apps/collect_data`：TCP 接收、解析、入库、接收日志写入
- `apps/receive_log`：接收日志列表与测试入口

---

## 数据接收流程

1. TCP 服务接收原始报文
2. 依据 `device_list.iridiumid -> sensorflag` 选择解析器
3. 写入对应业务表（按 `devid` 前缀映射表名）
4. 接收过程写入 `t_receive_log`
5. 去重规则：`iridiumid + sn`

---

## 关键数据流

- 首页地图点位：`/device/map/points/` -> 设备最新经纬度
- 设备详情：`/device/data/latest/`、`/device/data/trend/`、`/device/track/`
- 接收日志：`/receive_log/list/`

---

## 部署提示

- 建议业务表建立 `(devid, time)` 复合索引
- TCP 服务端口需放行
